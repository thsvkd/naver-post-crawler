# TODO

권장 후속 항목. 자동화 테스트로 덮이지 않아 **실기 확인이 필요한 것**과, 이번 범위 밖으로
미룬 것들을 남긴다. 각 항목에 "왜 지금 못 했는지"를 함께 적는다 — 그게 빠지면 다음 사람이
같은 판단을 처음부터 다시 한다.

## 실측이 필요한 것

### 웹뷰 로그인 1회의 쿠키 헤더 길이

Windows 자격 증명 관리자의 blob 한도는 2560바이트이고 UTF-16 저장이라 실질 1280자다
(`credentials.MAX_SECRET_BYTES`). 그런데 `format_cookie_header()`는 naver.com 쿠키를
**전부** 담으므로 한도를 넘을 수 있다. 넘으면 저장이 거부되고 사용자는 로그인이 유지되지
않는다.

- **할 일**: 앱의 '네이버 로그인'을 한 번 수행하고 저장되는 헤더의 길이를 잰다.
- **판정**: 1280자에 여유가 있으면 현행 유지. 근접하거나 넘으면 세션에 필요한 쿠키만
  저장하도록 범위를 좁히는 것(화이트리스트)을 재검토한다.
- **왜 지금 못 했나**: 실제 네이버 계정 로그인이 필요하다.
- 관련: 한도 초과 메시지는 "빈 세션에서 로그인하므로 보통 한도 안에 들어간다"고 안내하는데,
  이 문구도 위 실측으로 뒷받침되기 전까지는 근거가 없다.

### macOS Gatekeeper — 다운로드 경로

지금까지의 설치 검증은 로컬 파일에서 했다. GitHub에서 받으면 `com.apple.quarantine`이 붙어
Gatekeeper가 차단한다.

- **할 일**: 릴리스 페이지에서 `.pkg`를 직접 받아 설치해 본다.
- **판정**: README의 "시스템 설정 → 개인정보 보호 및 보안 → 확인 없이 열기" 안내대로
  통과되는지 확인한다. 안 되면 안내를 고친다.

### 배포본에서 keyring 백엔드가 실제로 잡히는지

지금까지의 보관소 검증은 개발 실행(`uv run`)에서 했다. **`flet build` 번들에서는 확인하지
않았다.** 번들에는 패키지 메타데이터(dist-info)가 없을 수 있어, keyring의 엔트리포인트
자동 탐색이 조용히 빈 백엔드를 고르는 것이 이 계층의 알려진 함정이다. 그래서
`credentials.platform_backend()`가 백엔드를 **명시 지정**하고 테스트로 고정해 두었지만,
번들에서 그 임포트가 실제로 성공하는지는 실기에서만 드러난다.

- **할 일**: 설치본을 띄워 '네이버 로그인'으로 쿠키를 저장하고, 다시 열었을 때 "저장된
  쿠키: 있음"이 뜨는지 확인한다.
- **왜 지금 못 했나**: 인터랙티브 데스크톱 세션이 필요하다. SSH로는 자격 증명 관리자가
  열리지 않고(`CredRead 1312`), GUI도 뜨지 않는다.

### ad-hoc 서명과 키체인 접근 프롬프트

macOS 키체인 항목의 접근 권한은 **서명 신원에 묶인다.** 우리 빌드는 ad-hoc 서명이라
업데이트로 신원이 바뀌면 "접근을 허용하시겠습니까" 프롬프트가 다시 뜰 수 있다.

- **할 일**: v0.1.2 설치본에서 로그인해 쿠키를 저장한 뒤, 다음 버전으로 OTA 업데이트하고
  프롬프트가 뜨는지 확인한다.
- **판정**: 매 업데이트마다 반복되면 정식 개발자 인증서 서명이 근본 해결이다. 프롬프트를
  거부하면 이관 로직이 `LOST`로 처리해 재로그인을 안내하므로 기능이 죽지는 않는다.

### 델타 업데이트

v0.1.1이 첫 Velopack 릴리스라 비교 대상이 없었다. v0.1.2 → 다음 버전에서 델타가 실제로
적용되는지 확인한다.

## 미룬 것

### Windows exe의 회사명이 flet 기본값 "Your Company"다

`scripts/build.py`가 `--org`만 넘기고 `--company`를 넘기지 않아, exe 속성과 **데이터 폴더
경로**에 그대로 드러난다: `%APPDATA%\Your Company\Naver Blog Backup`.

고치면 데이터 폴더 경로가 바뀌어 기존 사용자의 설정·로그를 이관해야 한다. 자격증명은 이미
OS 보관소에 있어 영향이 없으므로, 손실 위험은 설정 정도다. 다음 큰 버전에서 함께 정리하는
편이 낫다.

### macOS 첫 창 크기 깜빡임

Windows 러너는 `scripts/flet_template.py`가 첫 창 크기를 앱 크기로 패치하지만, macOS는 창
정의가 코드가 아니라 `MainMenu.xib`에 있어 별도 패치가 필요하다. Velopack 이관 당시 합의된
범위 밖으로 뒀다.

### v0.1.0 포터블 zip 사용자

구 업데이터가 `naver-post-crawler-windows.zip` 에셋만 찾는데 v0.1.1부터 그 에셋이 없다.
앱 안에서는 조용히 "최신"으로 판정되어 전환 안내를 받지 못한다. README와 릴리스 노트
안내에만 의존한다.

## 사소한 것

- Velopack의 실제 인자는 `--veloapp-obsoleted`인데 세 곳에 `-obsolete`로 적혀 있다:
  `tests/test_uninstall_hook.py:55`, `tests/test_flet_template.py:59`,
  `scripts/flet_template.py:88`(C++ 주석). 훅 판정이 부분 문자열 매칭이라 동작 결과는
  같지만 상수와 문서가 사실과 다르다.
- `tests/test_cookie_login.py::test_result_write_failure_does_not_escape`에 `assert`가 없다.
  "예외가 나지 않는다"가 계약이라 틀린 테스트는 아니지만, 로그 기록까지 단언하면 계약이
  분명해진다.
